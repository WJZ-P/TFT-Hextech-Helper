/**
 * 游戏区服类型
 * @description 与后端 SettingsStore 中 GameRegion 枚举的值保持一致
 */
export type GameRegion = 'CN' | 'NA';

/**
 * 客户端类型
 * @description 与后端 SettingsStore 中 GameClient 枚举的值保持一致
 */
export type GameClient = 'ANDROID' | 'RIOT_PC';
